name: repository size
run-name: ${{ github.actor }} asking for date
on: [push]
jobs:
  repo_size:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          repository: ''
      - run: du -skh --exclude=".git" 
